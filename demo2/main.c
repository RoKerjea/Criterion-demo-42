#include <stdio.h>
#include "project.h"

int	main(void)
{
	int	lol;
	lol = ft_machin(0);
	printf("Hello, i'm just here to demonstrate you can separate the functions to test from the main\n lol = %d\n", lol);
	return (0);
}